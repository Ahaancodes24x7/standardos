import { Link } from "@tanstack/react-router";
import { Menu } from "lucide-react";
import { Brand } from "@/components/brand";
import { Button } from "@/components/ui/button";
import { Sheet,SheetContent,SheetTrigger,SheetTitle } from "@/components/ui/sheet";
const links=[{label:"How it works",to:"/how-it-works" as const},{label:"Features",to:"/features" as const},{label:"Use cases",to:"/use-cases" as const},{label:"About",to:"/about" as const}];
export function PublicHeader(){return <header className="fixed inset-x-0 top-4 z-40 px-4"><div className="site-container"><div className="glass-panel flex h-16 items-center justify-between rounded-full px-4 transition-shadow duration-300 sm:px-6"><Brand compact/><nav className="hidden items-center gap-8 lg:flex" aria-label="Main navigation">{links.map((link)=><Link key={link.label} to={link.to} className="nav-link text-sm font-semibold" activeProps={{className:"nav-link text-sm font-semibold text-primary"}}>{link.label}</Link>)}</nav><div className="hidden items-center gap-1 sm:flex"><Button asChild variant="ghost" size="sm"><Link to="/login">Sign in</Link></Button><Button asChild size="sm" className="rounded-full px-5"><Link to="/signup">Get started</Link></Button></div><Sheet><SheetTrigger asChild><Button variant="ghost" size="icon" className="rounded-full sm:hidden" aria-label="Open navigation"><Menu/></Button></SheetTrigger><SheetContent side="right" className="glass-panel w-[86%]"><SheetTitle className="sr-only">Navigation</SheetTitle><Brand/><nav className="mt-10 grid gap-1">{links.map((link)=><Link key={link.label} to={link.to} className="border-b border-border py-4 font-semibold text-primary">{link.label}</Link>)}</nav><div className="mt-8 grid gap-3"><Button asChild variant="outline"><Link to="/login">Sign in</Link></Button><Button asChild><Link to="/signup">Get started</Link></Button></div></SheetContent></Sheet></div></div></header>}
